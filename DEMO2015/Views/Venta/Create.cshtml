<div class="container">
    <form action="/Venta/Store" method="post">

        <div class="form-group">
            <label for="" class="control-label">Número de Venta</label>

            <input type="text" name="NumeroVenta" class="form-control" id="NumeroVenta" placeholder="Número de Venta" value="1234" />
        </div>

        <div class="form-group">
            <label for="" class="control-lable">Fecha de Venta</label>
            <input type="text" class="form-control" name="FechaVenta" placeholder="Fecha de Venta" />
        </div>
        <div class="form-grpup">
            <label for="">Cliente</label>
        <select name="ClienteId" class="select form-control">
            <option value="1">Luis Mendoza Escalante</option>
            <option value="2">Napoleon Bonaparte</option>
            <option value="3">Adolf Hitler</option>
            <option value="4">Hugo Chavez</option>
            <option value="5">Nicolas Maduro</option>
        </select>
            </div>
        <div class="form-group">
            <label for="">Prducto</label>
            <select name="ProductoId" class="select form-control" id="product">
                <option value="1">Xbox One</option>
                <option value="2">PS4</option>
                <option value="3">Wii</option>
                <option value="4">Nintendo 3DS</option>
                <option value="5">Ouya</option>
            </select>
        </div>

        <div class="form-group">
            <a href="#" class="btn btn-default" id="as" onclick="addACarrito()">Añadir Producto</a>
        </div>
        



        <table class="table">
            <thead>
                <tr>
                    <th>N#</th>
                    <th>Producto</th>
                    <th>Stock</th>
                    <th>Precio</th>
                    <th>Cantidad</th>
                </tr>
            </thead>
            <tbody id="bodyProducto"></tbody>
        </table>

        <button class="btn btn-primary">Guardar Venta</button>

    </form>
</div>


<script>

    //$('#bodyProducto').html('<tr><td>N#</td><td>XboxOne</td><td>10</td><td>1500</td><td><input type="text" /></td></tr> ');
    

    //$('#as').html('ingresar Item');

    //alert($('#NumeroVenta').val());

    //$('#NumeroVenta').val('1235')

    var index = 0;


    function addACarrito()
    {
        // Necesitamos el valor/id de producto
        // Llamar al servidor http://localhost:4564/Venta/AddProducto/{id}
        // Servidor nos dara una respuesta y nostros capturaremos esa respuesta <tr><td>N#</td><td>XboxOne</td><td>10</td><td>1500</td><td><input type="text" /></td></tr>
        // esa respuesta la añadiremos en el bodyProducto $('#bodyProducto').append(respuesta);

        var valor = $('#product').val();
        
        $.get('/Venta/AddProducto/' + valor + '?index=' + index)
        .success(function (respuesta) {
            $('#bodyProducto').append(respuesta);
        });

        index++;
    }

    

    function mostrarValor()
    {
        var numero = $('#NumeroVenta').val();
        //http://localhost:4564/Venta/Multiplicar?numero=1234

        $.get('http://localhost:4564/Venta/Multiplicar?numero=' + numero)
        .success(function (respuesta)
        {
            alert(respuesta);
        });
    }
    




    
   
</script>